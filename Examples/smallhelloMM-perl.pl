$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
while($t[$mptr]) {
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
}
$mptr++;
if ($mptr == 256) { $mptr = 0; }
}
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
print chr($t[$mptr]);
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
print chr($t[$mptr]);
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
print chr($t[$mptr]);
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
print chr($t[$mptr]);
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$mptr++;
if ($mptr == 256) { $mptr = 0; }
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
print chr($t[$mptr]);
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$mptr = ($mptr == 0) ? 255 : $mptr - 1;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
