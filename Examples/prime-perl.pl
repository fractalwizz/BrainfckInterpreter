$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$t[$mptr]++;
while($t[$mptr]) {
$t[$mptr]--;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr] = ord(getc);
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$t[$mptr]--;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$t[$mptr]--;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
while($t[$mptr]) {
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
while($t[$mptr]) {
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
$mptr++;
if ($mptr == 25) { $mptr = 0; }
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr++;
if ($mptr == 25) { $mptr = 0; }
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
while($t[$mptr]) {
$t[$mptr]--;
}
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$mptr = ($mptr == 0) ? 24 : $mptr - 1;
$t[$mptr]--;
}
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
$t[$mptr]++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
}
