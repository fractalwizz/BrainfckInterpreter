while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
print chr($t[$mptr]);
}
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
while($t[$mptr]) {
$mptr--;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
}
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
while($t[$mptr]) {
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
}
$mptr++;
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
$mptr++;
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
$mptr++;
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
$mptr--;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
}
$mptr++;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr--;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
$mptr++;
$mptr++;
print chr($t[$mptr]);
$mptr--;
$mptr--;
}
$mptr++;
$mptr++;
$mptr++;
while($t[$mptr]) {
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
while($t[$mptr]) {
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
}
$mptr++;
print chr($t[$mptr]);
$mptr--;
$mptr--;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
$mptr--;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
}
$mptr++;
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
}
$mptr++;
}
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
$mptr--;
$mptr--;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr--;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
$mptr++;
$mptr++;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr--;
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$mptr++;
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr--;
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
$mptr++;
}
$mptr--;
$mptr--;
$mptr--;
}
$mptr--;
$mptr--;
$mptr--;
$mptr--;
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
print chr($t[$mptr]);
while($t[$mptr]) {
$t[$mptr]--;
if ($t[$mptr] < 0) { $t[$mptr] = 255; }
}
$mptr--;
}
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
$t[$mptr]++;
if ($t[$mptr] > 255) { $t[$mptr] = 0; }
